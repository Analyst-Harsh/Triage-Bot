# Engineering standards — full detail

The rationale behind `AGENTS.md`'s condensed "Engineering standards" list. Read this when a design decision needs a tiebreaker, or when a change would introduce a pattern that competes with one already established here.

- **Design patterns over raw/ad-hoc code.** When an established pattern fits, use it instead of hand-rolling something bespoke. This codebase already commits to specific patterns — follow them rather than introducing a competing style:
  - Discriminated unions for polymorphic data (`graph/schemas/actions.py`'s `DraftAction`), not loosely-typed dicts with optional fields for every variant.
  - A typed contract at every boundary (Pydantic models in/out of graph nodes, the `TriageState` TypedDict as the single source of truth for graph state) — see `docs/agent/architecture-conventions.md`.
  - Factory functions for non-trivial construction (`create_initial_state()`) instead of duplicating init logic at each call site.
  - One model/responsibility per file, re-exported from a package `__init__.py` (see the Module layout convention in `docs/agent/architecture-conventions.md`) — not one large file accumulating unrelated classes.
- **Prefer classes over standalone functions.** A lone, self-contained utility function — one clear purpose, no shared state, nothing else conceptually attached to it (e.g. `route_after_auto_post` in `graph/nodes/routing.py`) — is fine as a plain module-level function; forcing a one-method class around it is needless ceremony. But the moment a second function shows up that shares responsibility, state, or configuration with the first, group them onto a class instead of letting related functions accumulate as loose module-level definitions. This isn't about counting functions until some threshold is hit — it's about whether they belong together: `ActionExecutor` (`graph/nodes/utils/action_executor.py`) groups the comment/label/close/code-fix posting behaviors that would otherwise be four free functions passing the same `GitHubClient` around by hand; `DiffApplier` (`utils/diff_applier.py`) became a class specifically so `timeout_seconds` could be constructor-configured once and shared across every git invocation, rather than threading a parameter through a chain of free functions. If you're about to add a second function next to an existing one and they're going to call each other or share a parameter, that's the signal to consolidate immediately, not to wait for a third.
- **Validate at boundaries, trust internals.** Pydantic models validate anything crossing a node, API, or serialization boundary. Don't re-validate the same data deeper in business logic once it's already a validated model.
- **Type-complete by default.** Every function gets a real return type annotation; strict `pyright` passing is the bar, not an aspiration. Avoid `Any` as a shortcut — it's acceptable only where genuinely unavoidable (e.g. heterogeneous test-fixture kwargs, see the Typing convention in `docs/agent/architecture-conventions.md`), and only with the same explicit, deliberate annotation used there.
- **Tests are part of the definition of done**, not a follow-up task. New schemas/models get the same coverage shape as existing ones (construction + JSON round-trip at minimum; full checkpoint-serde round-trip for anything touching `TriageState` — see the Testing convention in `docs/agent/architecture-conventions.md`).
- **Lint, format, and type-check clean before considering work finished.** These are enforced by git hooks (see `lefthook.yml`), but don't treat the hook as the first line of defense — run `ruff check .` / `ruff format --check .` / `pyright` yourself and fix what they find as part of doing the work, not as a separate cleanup pass.
- **No silent shortcuts.** No "TODO: fix later," no swallowed exceptions, no disabled lint/type rules to make something pass — if a rule genuinely shouldn't apply, say why inline (see the `S101`/`tests/**` and `reportMissingTypeStubs` precedents in `pyproject.toml`) rather than blanket-disabling it.
- **No test-only constructor parameters.** A class's constructor is shaped by what it needs in production, never by what makes it easy to test. A class that needs a real dependency (an API client, a settings-derived secret) builds it itself in `__init__` — via `Settings`/`get_settings()`, the way `LLMNode` and `GitHubClient` (`utils/github_client.py`) do — rather than accepting it as an injectable parameter whose only actual consumer is the test suite. When a test needs to substitute behavior, it subclasses the real class and overrides `__init__` to accept a fake directly — the `_Fake<Name>` pattern already used throughout `tests/graph/nodes/conftest.py` (`_FakePlannerNode`, `_FakeRiskCheckNode`, `_FakeDrafterSubgraph`, `_FakeAutoPostNode`) and `tests/utils/test_github_client.py` (`_FakeGitHubClient`) — or mocks/stubs the specific boundary being exercised. Secrets are read exclusively through `Settings`/`get_settings()`, never `os.environ` directly, outside `config/settings.py` itself.
- **When there's more than one way to build something, pick the one a senior engineer shipping this to production would pick** — not the one that's fastest to type out. If that's a nontrivial judgment call, it's worth a sentence of reasoning in the code (a comment) or the commit, not just a silent choice.
