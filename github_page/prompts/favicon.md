A minimal, modern app-icon mark for an AI developer-tool brand called "Triage Bot".

IMPORTANT — the previous attempt at this prompt rendered as a soft blurred
gradient orb, which does not work as a favicon. This must NOT happen again:
no gaussian blur, no soft bokeh glow, no diffuse light bloom, no depth of
field. Every edge must be crisp and hard, like a simple app icon or line-art
logo mark — think Material Design icon or a minimal SaaS product mark, not a
photographic light effect.

Design: a single abstract geometric glyph suggesting a signal or triage
checkpoint — a concentric-ring "target/node" shape (a small filled circle
inside a ring, like a radar ping or a map pin dot) works well. Flat, solid
color fills only, no gradients, no text, no photorealism. The silhouette must
read clearly and unambiguously even reduced to 16x16px — one dominant shape,
no fine detail, no thin hairlines that would disappear at small sizes.

Colors: solid violet (#7C3AED) as the primary shape, solid blue (#60A5FA) as
one small accent (e.g. the center dot), on a solid dark slate (#0F172A) or
transparent background. No gradient blends between the colors — each shape is
a single flat color.

Format: square 512x512 canvas, generous padding, centered. No drop shadows, no
bevels, no 3D render, no glow/blur of any kind — pure flat vector mark,
comparable in crispness to a simple line-icon SVG, exportable down to a 16px
favicon without turning into a smudge.
