A bold, rich centerpiece illustration for the hero section of an AI GitHub-
issue-triage agent's landing page, called "Triage Bot". This should feel like
the single "hero object" of the page — the way a striking 3D-rendered product
shot anchors a landing page — not a background texture or pattern.

Subject: an abstract "signal core" — a glowing, faceted geodesic/crystalline
sphere or node-network sculpture, built from interconnected glowing nodes and
thin light-trail connections, as if visualizing an AI agent's decision graph.
Rich dramatic lighting, some depth and dimensionality (soft ambient occlusion,
subtle rim lighting), volumetric glow around the brightest nodes — this one
CAN have glow/bloom, unlike the favicon, since it's meant to look like a
premium 3D render, not a small crisp icon.

Colors: a gradient across violet (#7C3AED), indigo (#6366F1), and blue
(#60A5FA), on a deep slate-navy (#0F172A) backdrop or fully transparent
background. No other colors — stay within this palette.

Composition: the core object centered or slightly off-center, with room
around the edges (this will be layered over an existing animated particle
background and headline text in code, so keep the core visually self-
contained rather than filling the whole frame edge-to-edge). No text, no
logos, no human figures, no literal robot/hardware imagery — this is an
abstract representation of "an AI agent's reasoning," not a mascot.

Format: square or slightly-tall canvas, at least 1600x1600, transparent
background (PNG) so it composites cleanly over the existing dark hero.
